#!/bin/bash

current_file=$1
dest_file=$2

result=$(check-backlink $current_file $dest_file)

if [[ "$result" == "no backlink" ]]; then
	backlink_text="$(get-link $current_file){.back}"
	echo -e "\n$backlink_text" >> $dest_file
	set-date "updated" $dest_file
else
	echo $result
fi
