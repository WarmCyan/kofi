#!/bin/bash

filename=$1
outname=$(echo $filename | sed "s/\.md$/\.html/")

echo $outname

#pushd $(active-dir) > /dev/null

pandoc -i $filename -o rendered/$outname --lua-filter=$BIN_DIR/kofi/links-to-html.lua --template=$BIN_DIR/kofi/assets/template.html

#popd > /dev/null
