INPUTS := $(wildcard *.md)
TARGETS := $(patsubst %.md,rendered/%.html,$(notdir $(INPUTS)))

all: $(TARGETS)

rendered/%.html: %.md | rendered
	@render $<

rendered:
	@mkdir -p rendered

clean:
	rm rendered/*

.PHONY: all clean
